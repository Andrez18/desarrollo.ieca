---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Layout from "../layouts/Layout.astro";

const images = [
  {
    url: "/slider-1.png",
    alt: "Primera imagen del slider",
  },
  {
    url: "/slider-2.png",
    alt: "Segunda imagen del slider",
  },
  {
    url: "/slider-3.png",
    alt: "Tercera imagen del slider",
  },
  {
    url: "/slider-4.png",
    alt: "Cuarta imagen del slider",
  },
  {
    url: "/slider-5.png",
    alt: "Quinta imagen del slider",
  },
];
---

<Layout title="Inicio - Desarrollo IECA">
  <section
    class="relative min-h-screen bg-cover bg-center bg-no-repeat"
    style="background-image: url('/desarrollo-home.png');"
  >
    <div class="absolute inset-0 bg-black/30"></div>
    <Header class="relative z-20" />

    <div
      class="relative z-10 container mx-auto px-4 sm:px-6 lg:px-8 py-8 sm:py-16 text-[var(--text-light)]"
    >
      <div
        class="flex flex-col lg:grid lg:grid-cols-2 gap-6 sm:gap-8 lg:gap-16 items-start min-h-[calc(100vh-200px)]"
      >
        <div
          class="order-1 flex flex-col justify-center space-y-4 sm:space-y-6 lg:space-y-12"
        >
          <div class="space-y-3 sm:space-y-4 lg:space-y-6">
            <h1
              class="text-2xl sm:text-3xl md:text-4xl lg:text-5xl xl:text-6xl 2xl:text-7xl font-bold leading-tight"
            >
              DESARROLLANDO CALIDAD
            </h1>

            <p
              class="text-[var(--text-blue-dark)] text-xs sm:text-sm lg:text-base xl:text-lg uppercase tracking-wide lg:tracking-wider font-medium max-w-lg mb-9"
            >
              NOS ENFOCADOS EN EL DESARROLLO Y LA EDUCACIÓN CON CALIDAD EN LA
              IECA
            </p>

            <div class="mt-8 sm:mt-6 lg:mt-14 xl:mt-38">
              <p
                class="text-[var(text-light)] text-xs sm:text-sm lg:text-[15px] leading-relaxed max-w-md font-regular"
              >
                Estudiar desarrollo de software en la IECA no es solo aprender a
                programar, se construye un camino, crear sus propuestas y nunca
                olvidar el poder de lo que te hace único en este mundo digital.
              </p>
            </div>
          </div>
        </div>

        <div
          class="order-2 flex flex-col-reverse lg:flex-col justify-between h-full space-y-6 sm:space-y-8 space-y-reverse lg:space-y-8 lg:space-y-reverse-0"
        >
          <div class="text-center lg:text-right">
            <div
              class="text-7xl sm:text-8xl md:text-5xl lg:text-6xl xl:text-8xl 2xl:text-8xl font-medium text-[var(--text-light)] tracking-tight"
            >
              <span id="year"></span>
            </div>
            <p
              id="clock"
              class="text-2xl sm:text-base lg:text-lg xl:text-xl text-[var(--text-blue-dark)] mt-1 sm:mt-2 lg:mt-4 font-medium"
            >
            </p>
          </div>
          <div
            class="bg-white/95 backdrop-blur-sm text-black rounded-2xl p-4 sm:p-6 lg:p-8 mx-auto lg:ml-auto lg:mr-0 max-w-sm w-full shadow-2xl"
          >
            <h3
              class="text-base sm:text-lg lg:text-xl font-bold mb-2 sm:mb-3 text-gray-900"
            >
              BLOG
            </h3>
            <p
              class="text-xs sm:text-sm text-gray-600 mb-4 sm:mb-6 leading-relaxed"
            >
              Descubre las últimas noticias relacionadas con la Media Técnica en
              la ieca
            </p>
            <a
              href="/blog"
              class="group bg-[#131313] text-white px-4 sm:px-6 py-2 rounded-full text-sm sm:text-lg font-medium transition-all duration-300 flex items-center gap-3 w-full justify-center hover:opacity-95 hover:shadow-lg"
            >
              VER AHORA
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section
    class="relative min-h-screen bg-black text-[var(--text-light)] overflow-hidden"
  >
    <div
      class="absolute inset-0 bottom-85 flex items-center justify-center pointer-events-none"
    >
      <h2
        class="text-6xl sm:text-7xl md:text-8xl lg:text-[128px] font-bowlbyOne font-bold text-[var(--background-light)] select-none leading-none text-center"
      >
        PROYECTOS
      </h2>
    </div>

    <div
      class="absolute inset-0 flex items-center justify-center pointer-events-none overflow-hidden"
    >
      <h3
        class="text-6xl sm:text-7xl md:text-8xl lg:text-[128px] font-bowlbyOne font-bold text-[var(--background-light)] select-none leading-none whitespace-nowrap"
      >
        DESTACADOS
      </h3>
    </div>

    <div
      class="relative z-10 px-4 sm:px-6 lg:px-12 py-8 sm:py-12 max-w-7xl mx-auto"
    >
      <div class="grid lg:grid-cols-2 gap-8 lg:gap-12 min-h-screen">
        <div class="flex flex-col justify-between order-2 lg:order-1">
          <div class="space-y-6 mt-8 sm:mt-16 lg:mt-48">
            <div>
              <h3
                class="text-[var(--text-blue-dark)] text-xs sm:text-sm font-medium mb-4 tracking-wider"
              >
                • SOBRE ESTE
              </h3>
              <p
                class="text-gray-300 text-sm leading-relaxed max-w-sm lg:max-w-[294px] font-medium"
              >
                Desarrollo de un sitio web acerca de la media técnica de la
                misma institución.
              </p>
            </div>
          </div>

          <div class="pb-8 mt-8 lg:mt-0">
            <h4
              class="text-[var(--text-blue-dark)] text-xs sm:text-sm font-medium mb-4 tracking-wider"
            >
              / ÚLTIMO PROYECTO 2025
            </h4>
            <p class="text-gray-300 text-sm leading-relaxed mb-6 max-w-md">
              Se desarrolló un sitio web para obtener información más a la mano
              sobre la Media Técnica para demostrar con el mismo sitio web lo
              que se construye dentro de la misma, accediendo a información
              relevante como los módulos y algunos proyectos realizados.
            </p>
            <a
              href="#"
              class="inline-flex items-center text-[var(--text-light)] text-sm font-medium hover:text-[var(--text-blue-light)] transition-colors group"
            >
              IR A LOS PROYECTOS
              <svg
                class="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  strokeLinecap="round"
                  strokeLinejoin="round"
                  strokeWidth={2}
                  d="M9 5l7 7-7 7"></path>
              </svg>
            </a>
          </div>
        </div>

        <div
          class="flex flex-col justify-center space-y-6 sm:space-y-8 order-1 lg:order-2"
        >
          <div class="pt-4 sm:pt-8">
            <h2
              class="text-2xl sm:text-3xl lg:text-4xl xl:text-5xl 2xl:text-6xl font-semibold leading-tight mb-8 sm:mb-12"
            >
              <span
                class="text-[var(--text-grey-light)] text-lg sm:text-xl lg:text-2xl text-center align-middle mr-4 sm:mr-8 lg:mr-12"
                >/ 01</span
              >
              ENTRAR A LA MEDIA TÉCNICA ES MÁS QUE SOLO UN TÍTULO.
            </h2>
          </div>

          <div class="flex flex-row gap-7 justify-center items-center">
            <div class="relative flex justify-center lg:justify-start">
              <img
                src="/projects-home-1.png"
                alt="Colaboración en oficina moderna"
                class="w-full max-w-[362px] h-[240px] sm:h-[300px] lg:h-[360px] object-cover rounded-lg"
              />
            </div>

            <div class="relative flex justify-center lg:justify-start">
              <div
                class="flex flex-col sm:flex-row items-center sm:items-end space-y-4 sm:space-y-0 sm:space-x-4"
              >
                <img
                  src="/projects-home-2.png"
                  alt="Manos escribiendo en teclado"
                  class="w-full max-w-[362px] h-[150px] sm:h-[195px] object-cover rounded-lg"
                />
              </div>
            </div>
          </div>
          <div class="text-center sm:text-right space-y-2 flex-shrink-0">
            <div class="text-gray-400 text-xs tracking-wider">
              WEB<br />
              MEDIA TÉCNICA<br />
              <span class="font-bold">DESARROLLO</span>
            </div>
            <div
              class="text-[var(--text-blue-dark)] text-xl sm:text-2xl font-bold"
            >
              2025
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="bg-[#373733] min-h-screen">
    <div class="p-4 sm:p-6">
      <!-- Header -->
      <header
        class="flex flex-col sm:flex-row justify-between items-center sm:items-start mb-6 sm:mb-12 gap-3 sm:gap-0 px-2 text-center sm:text-left"
      >
        <p
          class="text-xs text-[#0a4f9a] font-semibold uppercase leading-none tracking-widest"
        >
          <span class="inline-block mr-1">•</span>Nuestra Calidad
        </p>
        <h2 class="text-white font-bold text-2xl sm:text-4xl leading-tight">
          REPRESENTADOS POR NUESTRA CALIDAD
        </h2>
        <div class="text-gray-400 font-light tracking-wide select-none">
          / 02
        </div>
      </header>

      <div class="relative flex justify-center">
        <img
          alt="Dos personas trabajando en un escritorio"
          class="w-full max-w-[90%] object-cover rounded-lg"
          src="/image-6.png"
        />
        <div
          class="absolute top-3 left-6 sm:left-20 text-sm sm:text-base text-gray-300 font-light leading-tight select-none max-w-[250px] sm:max-w-[325px]"
        >
          Reconocidos por nuestra ardua labor en el mundo del desarrollo
        </div>
      </div>

      <div
        class="flex flex-col sm:flex-row justify-between items-center sm:items-start mt-8 sm:mt-10 px-2 gap-6"
      >
        <div
          class="text-sm text-gray-400 font-light max-w-full sm:max-w-[50%] leading-tight select-none flex flex-col gap-3"
        >
          <a
            href="/modules"
            class="font-bold uppercase underline text-base hover:text-white"
          >
            Ir a los módulos
          </a>
          <p>
            El lugar donde la calidad y la innovación se unen para crear
            experiencias únicas, impulsando ideas que transforman, inspiran y
            marcan la diferencia en cada proyecto que desarrollamos con pasión.
          </p>
        </div>
        <div
          class="text-2xl sm:text-4xl text-white text-center sm:text-right max-w-full sm:max-w-[35%] leading-tight select-none"
        >
          CON LOS ESTÁNDARES MÁS ALTOS
        </div>
      </div>

      <div
        class="flex flex-col sm:flex-row justify-center items-center mt-6 px-2 gap-4 text-xs"
      >
        <a
          href="https://www.ieciudadelalasamericas.edu.co/"
          target="_blank"
          rel="noopener noreferrer"
          class="border border-gray-400 font-semibold uppercase tracking-widest py-2 px-6 w-full sm:w-[40%] lg:w-[15%] hover:bg-gray-700 transition-colors text-center"
        >
          Ciudadela Las Américas
        </a>

        <a
          href="https://pascualbravo.edu.co/"
          target="_blank"
          rel="noopener noreferrer"
          class="border border-gray-400 font-semibold uppercase tracking-widest py-2 px-6 w-full sm:w-[40%] lg:w-[15%] hover:bg-gray-700 transition-colors text-center"
        >
          Pascual Bravo
        </a>
      </div>
    </div>
  </section>

  <section class="px-4 sm:px-8 py-12 max-w-[1200px] mx-auto">
    <header
      class="flex flex-col md:flex-row justify-between items-center md:items-start mb-12 gap-6 md:gap-0 text-center md:text-left"
    >
      <div>
        <div class="text-gray-400 font-light tracking-wide select-none">
          / 03
        </div>
        <span class="text-3xl sm:text-4xl">PARA</span>
      </div>

      <svg
        class="w-6 h-6 text-blue-700 mx-auto md:mx-0"
        fill="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          d="M12 2L14.09 8.26L21 9.27L16 14.14L17.18 21.02L12 17.77L6.82 21.02L8 14.14L3 9.27L9.91 8.26L12 2Z"
        ></path>
      </svg>

      <h2
        class="text-2xl sm:text-3xl md:text-4xl font-bold max-w-xl leading-tight"
      >
        HABILIDADES ÚNICAS.<br />UN FUTURO ASEGURADO.
      </h2>

      <div class="max-w-[175px] text-center md:text-right">
        <h2 class="font-semibold text-2xl sm:text-4xl mb-1">A MEDIDA</h2>
        <p class="text-xs leading-snug text-[#6F706B]">
          Forma parte de la técnica para graduarte con el título bachiller
          técnico
        </p>
      </div>
    </header>

    <div
      class="flex flex-col md:flex-row md:space-x-12 items-center md:items-start"
    >
      <img
        src="/image-7.png"
        alt="Personas trabajando en programación"
        class="w-full max-w-md rounded-lg mb-6 md:mb-0"
      />

      <article class="md:w-2/3 text-gray-300 space-y-6">
        <p class="text-sm text-blue-700 font-semibold inline-block mb-2">
          ¿POR QUÉ ELEGIRNOS?
        </p>

        <ol class="space-y-8 list-none">
          <li class="max-w-full md:max-w-[70%]">
            <h2 class="font-semibold text-2xl sm:text-3xl text-[#6F706B]">
              <span class="text-[#373733] text-sm">/ 01</span> CONVENIO CON LA UNIVERSIDAD
              PASCUAL BRAVO
            </h2>
          </li>

          <hr class="text-[#232323]" />

          <li class="max-w-full md:max-w-[70%] ml-5">
            <h2 class="font-semibold text-2xl sm:text-3xl">
              <span class="text-[#373733] text-sm">/ 02</span> GRADÚATE COMO BACHILLER
              TÉCNICO
            </h2>
            <p class="text-sm leading-relaxed text-[#6F706B]">
              Al estar en nuestra Media Técnica te graduarás como bachiller
              técnico en lugar de bachiller académico y encadenas directo con el
              pascual bravo para seguir tus sueños
            </p>
          </li>

          <hr class="text-[#232323]" />

          <li class="max-w-full md:max-w-[70%]">
            <h2 class="font-semibold text-2xl sm:text-3xl text-[#6F706B]">
              <span class="text-[#373733] text-sm">/ 03</span> LA OPORTUNIDAD DE
              TRABAJAR CON CONTRATOS INTERNACIONALES
            </h2>
          </li>
        </ol>

        <hr class="text-[#232323]" />

        <h3
          class="text-5xl sm:text-7xl font-semibold text-[#0D0D0D] mt-12 select-none leading-none"
        >
          ELÍGENOS AHORA
        </h3>
      </article>
    </div>
  </section>

  <section class="slider-container bg-black py-16 overflow-hidden relative">
    <button
      id="prevBtn"
      class="absolute left-4 top-1/2 transform -translate-y-1/2 z-10 bg-white/20 cursor-pointer hover:bg-white/30 text-white p-3 rounded-full backdrop-blur-sm transition-all duration-300"
      aria-label="Imagen anterior"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M15 19l-7-7 7-7"></path>
      </svg>
    </button>

    <button
      id="nextBtn"
      class="absolute right-4 top-1/2 transform -translate-y-1/2 z-10 bg-white/20 cursor-pointer hover:bg-white/30 text-white p-3 rounded-full backdrop-blur-sm transition-all duration-300"
      aria-label="Imagen siguiente"
    >
      <svg
        class="w-6 h-6"
        fill="none"
        stroke="currentColor"
        viewBox="0 0 24 24"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M9 5l7 7-7 7"></path>
      </svg>
    </button>

    <div class="slider-wrapper overflow-hidden">
      <div
        id="sliderTrack"
        class="slider-track flex transition-transform duration-500 ease-in-out"
      >
        <div
          class="slide flex-shrink-0 mx-2 min-w-[320px] md:min-w-[280px] sm:min-w-[240px]"
        >
          <img
            src={images[images.length - 1].url}
            alt={images[images.length - 1].alt}
            class="w-80 h-48 md:w-72 md:h-44 sm:w-60 sm:h-40 object-cover rounded-lg shadow-xl"
            loading="lazy"
          />
        </div>

        {
          images.map((image) => (
            <div class="slide flex-shrink-0 mx-2 min-w-[320px] md:min-w-[280px] sm:min-w-[240px]">
              <img
                src={image.url}
                alt={image.alt}
                class="w-80 h-48 md:w-72 md:h-44 sm:w-60 sm:h-40 object-cover rounded-lg shadow-xl hover:shadow-2xl transition-all duration-300 hover:scale-105"
                loading="lazy"
              />
            </div>
          ))
        }

        <div
          class="slide flex-shrink-0 mx-2 min-w-[320px] md:min-w-[280px] sm:min-w-[240px]"
        >
          <img
            src={images[0].url}
            alt={images[0].alt}
            class="w-80 h-48 md:w-72 md:h-44 sm:w-60 sm:h-40 object-cover rounded-lg shadow-xl"
            loading="lazy"
          />
        </div>
      </div>
    </div>
  </section>

  <Footer />

  <script client:load>
    function updateClock() {
      const now = new Date();

      const dias = ["DOM", "LUN", "MAR", "MIE", "JUE", "VIE", "SAB"];
      const diaSemana = dias[now.getDay()];
      const diaMes = now.getDate();
      const horas = String(now.getHours()).padStart(2, "0");
      const minutos = String(now.getMinutes()).padStart(2, "0");
      const anio = now.getFullYear();

      document.getElementById("year").innerText = anio;
      document.getElementById("clock").innerText =
        `${diaSemana} ${diaMes} \\ ${horas}:${minutos}`;
    }

    updateClock();
    setInterval(updateClock, 1000);

    document.addEventListener("DOMContentLoaded", () => {
      const sliderTrack = document.getElementById("sliderTrack");
      const prevBtn = document.getElementById("prevBtn");
      const nextBtn = document.getElementById("nextBtn");
      const slides = document.querySelectorAll(".slide");

      let currentIndex = 1;
      let slideWidth = slides[0].offsetWidth + 16 * 2;
      sliderTrack.style.transform = `translateX(-${currentIndex * slideWidth}px)`;

      function moveToIndex(index) {
        sliderTrack.style.transition = "transform 0.5s ease-in-out";
        currentIndex = index;
        sliderTrack.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
      }

      function nextSlide() {
        if (currentIndex >= slides.length - 1) return;
        moveToIndex(currentIndex + 1);
      }

      function prevSlide() {
        if (currentIndex <= 0) return;
        moveToIndex(currentIndex - 1);
      }

      nextBtn.addEventListener("click", nextSlide);
      prevBtn.addEventListener("click", prevSlide);

      sliderTrack.addEventListener("transitionend", () => {
        if (currentIndex === slides.length - 1) {
          sliderTrack.style.transition = "none";
          currentIndex = 1;
          sliderTrack.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
        }
        if (currentIndex === 0) {
          sliderTrack.style.transition = "none";
          currentIndex = slides.length - 2;
          sliderTrack.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
        }
      });

      window.addEventListener("resize", () => {
        slideWidth = slides[0].offsetWidth + 16 * 2;
        sliderTrack.style.transform = `translateX(-${currentIndex * slideWidth}px)`;
      });

      let startX = 0;
      let endX = 0;

      sliderTrack.addEventListener("touchstart", (e) => {
        startX = e.touches[0].clientX;
      });

      sliderTrack.addEventListener("touchend", (e) => {
        endX = e.changedTouches[0].clientX;
        const diff = startX - endX;
        if (Math.abs(diff) > 50) {
          if (diff > 0) nextSlide();
          else prevSlide();
        }
      });

      document.addEventListener("keydown", (e) => {
        if (e.key === "ArrowLeft") prevSlide();
        if (e.key === "ArrowRight") nextSlide();
      });
    });
  </script>
</Layout>
