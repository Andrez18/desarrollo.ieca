---
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import Layout from '../layouts/Layout.astro';
---

<Layout title="Inicio - Desarrollo IECA">
	<section class="relative min-h-screen bg-cover bg-center bg-no-repeat" 
		 style="background-image: url('/desarrollo-home.png');">
		 
		<div class="absolute inset-0 bg-opacity-70 pointer-events-none"></div>
		<Header class="relative z-10" />
		
		<div class="relative z-10 px-4 sm:px-6 lg:px-12 py-8 sm:py-12 max-w-7xl mx-auto text-[var(--text-light)]">
			<div class="grid lg:grid-cols-2 gap-8 lg:gap-12 items-start">
				<!-- Columna izquierda -->
				<div class="space-y-6 sm:space-y-8">
					<div>
						<h1 class="text-4xl sm:text-5xl lg:text-6xl font-bold mb-3 leading-tight" id="home">
							DESARROLLANDO<br>
							<span>CALIDAD</span>
						</h1>
						
						<p class="text-[var(--text-blue-dark)] text-sm sm:text-base lg:text-[18px] uppercase tracking-[1px] sm:tracking-[2px] mb-12 sm:mb-24 lg:mb-[280px] leading-relaxed">
							ENFOCADOS EN EL DESARROLLO Y<br class="hidden sm:block">
							LA EDUCACIÓN CON CALIDAD
						</p>
						
						<p class="text-[var(text-light)] text-sm sm:text-[15px] leading-relaxed max-w-md font-regular">
							Estudiar desarrollo de software no es solo aprender a programar, 
							se construye un camino, crear sus propuestas y nunca olvidar el 
							poder de lo que te hace único en este mundo digital.
						</p>
					</div>
				</div>

				<!-- Columna derecha -->
				<div class="space-y-8 sm:space-y-12">
					<!-- Año y reloj -->
					<div class="text-center lg:text-right">
						<div class="text-6xl sm:text-7xl lg:text-8xl xl:text-9xl font-medium text[var(--text-light)] tracking-[-3px] sm:tracking-[-7px]">
							<span id="year"></span>
						</div>
						<p id="clock" class="text-lg sm:text-[20px] text-[var(--text-blue-dark)] mt-4 sm:mt-[25px]"></p>
					</div>

					<!-- Card del blog -->
					<div class="bg-white text-black rounded-lg p-4 sm:p-6 mx-auto lg:ml-auto lg:mr-0 max-w-sm mt-8 sm:mt-12 lg:mt-[200px]">
						<h3 class="text-lg sm:text-xl font-bold mb-2">BLOG</h3>
						<p class="text-xs sm:text-sm text-[#373733] mb-4">
							Descubre de los últimos noticias<br>
							relacionados con la media técnica
						</p>
						<button class="bg-[#131313] text-[var(--text-light)] px-4 sm:px-6 py-2 rounded-full text-xs sm:text-sm transition-colors flex items-center gap-2 w-full sm:w-auto justify-center sm:justify-start">
							VER AHORA
							<svg class="w-6 h-6 sm:w-8 sm:h-8 p-1 sm:p-2 rounded-full bg-[var(--text-blue-light)] hover:ml-1.5 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24">
								<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
							</svg>
						</button>
					</div>
				</div>
			</div>
		</div>
	</section>

	<section class="relative min-h-screen bg-black text-[var(--text-light)] overflow-hidden">
		<div class="absolute inset-0 flex items-center justify-center pointer-events-none">
			<h2 class="text-6xl sm:text-7xl md:text-8xl lg:text-[128px] font-bowlbyOne font-bold text-[var(--background-light)] select-none leading-none text-center">
				PROYECTOS
			</h2>
		</div>

		<div class="absolute bottom-0 left-0 pointer-events-none overflow-hidden">
			<h3 class="text-6xl sm:text-7xl md:text-8xl lg:text-[128px] font-bowlbyOne font-bold text-[var(--background-light)] select-none leading-none whitespace-nowrap">
				DESTACADOS
			</h3>
		</div>

		<div class="relative z-10 px-4 sm:px-6 lg:px-12 py-8 sm:py-12 max-w-7xl mx-auto">
			<div class="grid lg:grid-cols-2 gap-8 lg:gap-12 min-h-screen">
				<div class="flex flex-col justify-between order-2 lg:order-1">
					<div class="space-y-6 mt-8 sm:mt-16 lg:mt-48">
						<div>
							<h3 class="text-[var(--text-blue-dark)] text-xs sm:text-sm font-medium mb-4 tracking-wider">• SOBRE ESTE</h3>
							<p class="text-gray-300 text-sm leading-relaxed max-w-sm lg:max-w-[294px] font-medium">
								Desarrollo de un sitio web acerca de la media técnica de la misma institución.
							</p>
						</div>
					</div>
					
					<div class="pb-8 mt-8 lg:mt-0">
						<h4 class="text-[var(--text-blue-dark)] text-xs sm:text-sm font-medium mb-4 tracking-wider">/ ÚLTIMO PROYECTO 2025</h4>
						<p class="text-gray-300 text-sm leading-relaxed mb-6 max-w-md">
							Se desarrolló un sitio web para obtener información más a la mano sobre la Media Técnica para
							demostrar con el mismo sitio web lo que se construye dentro de la misma, accediendo a información
							relevante como los módulos y algunos proyectos realizados.
						</p>
						<a
							href="#"
							class="inline-flex items-center text-[var(--text-light)] text-sm font-medium hover:text-[var(--text-blue-light)] transition-colors group"
						>
							IR A LOS PROYECTOS
							<svg
								class="ml-2 w-4 h-4 group-hover:translate-x-1 transition-transform"
								fill="none"
								stroke="currentColor"
								viewBox="0 0 24 24"
							>
								<path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M9 5l7 7-7 7" />
							</svg>
						</a>
					</div>
				</div>

				<div class="flex flex-col justify-center space-y-6 sm:space-y-8 order-1 lg:order-2">
					<div class="pt-4 sm:pt-8">
						<h1 class="text-2xl sm:text-3xl lg:text-4xl xl:text-5xl 2xl:text-6xl font-semibold leading-tight mb-8 sm:mb-12">
							<span class="text-[var(--text-grey-light)] text-lg sm:text-xl lg:text-2xl text-center align-middle mr-4 sm:mr-8 lg:mr-12">/ 01</span>
							ENTRAR A LA<br />
							MEDIA TÉCNICA ES<br />
							MÁS QUE SOLO UN<br />
							TÍTULO.
						</h1>
					</div>

					<div class="relative flex justify-center lg:justify-start">
						<img
							src="/projects-home-1.png"
							alt="Colaboración en oficina moderna"
							class="w-full max-w-[362px] h-[240px] sm:h-[300px] lg:h-[360px] object-cover rounded-lg"
						/>
					</div>

					<div class="relative flex justify-center lg:justify-start">
						<div class="flex flex-col sm:flex-row items-center sm:items-end space-y-4 sm:space-y-0 sm:space-x-4">
							<img
								src="/projects-home-2.png"
								alt="Manos escribiendo en teclado"
								class="w-full max-w-[362px] h-[150px] sm:h-[195px] object-cover rounded-lg"
							/>
							<div class="text-center sm:text-right space-y-2 flex-shrink-0">
								<div class="text-gray-400 text-xs tracking-wider">
									WEB<br />
									MEDIA TÉCNICA<br />
									DESARROLLO
								</div>
								<div class="text-[var(--text-blue-dark)] text-xl sm:text-2xl font-bold">2025</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</section>

	<Footer />

	<script client:load>
		function updateClock() {
			const now = new Date();

			const dias = ["DOM", "LUN", "MAR", "MIE", "JUE", "VIE", "SAB"];
			const diaSemana = dias[now.getDay()];
			const diaMes = now.getDate();
			const horas = String(now.getHours()).padStart(2, "0");
			const minutos = String(now.getMinutes()).padStart(2, "0");
			const anio = now.getFullYear();

			document.getElementById("year").innerText = anio;
			document.getElementById("clock").innerText = 
				`${diaSemana} ${diaMes} \\ ${horas}:${minutos}`;
		}

		updateClock();
		setInterval(updateClock, 1000); 
	</script>
</Layout>